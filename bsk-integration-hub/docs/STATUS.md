# Статус проекта (передача контекста между сессиями)

> Этот файл — «память» проекта. Новая сессия Claude Code должна прочитать его
> первым и продолжить работу с раздела «Текущая задача». Обновлять при каждом
> существенном шаге.

Обновлено: 2026-06-10.

## Что развёрнуто

- Прод: **https://hub.bsk-grupp.ru** (VPS Beget, IP 45.67.58.89), стек
  Docker Compose: `docker-compose.yml` + `docker-compose.prod.yml` (Caddy с HTTPS).
- **Авто-деплой**: push в `main` → GitHub Actions (`.github/workflows/deploy.yml`)
  → SSH → `scripts/beget-update.sh` (git reset --hard origin/main + rebuild).
  Деплой НЕ трогает серверный `.env`.
- Портал Bitrix24: **basht.bitrix24.ru**. МойСклад — боевой аккаунт.

## Режим безопасности (КРИТИЧНО)

- Подключены **боевые** доступы Bitrix24 и МойСклад (тестовых нет!).
  Слова владельца: «я даю сразу боевые, будь осторожен!».
- На сервере в `.env`: `USE_MOCK_CONNECTORS=false`, `ALLOW_REAL_API=true`,
  `DRY_RUN=true` → **реальные чтения разрешены, записи невозможны**.
- Любое включение записи — только после явного подтверждения владельца.
- Секреты — только в `.env` на сервере (в git их нет). `ADMIN_API_TOKEN` —
  спросить у владельца, в репозитории он не хранится.
- Правило процесса: **всегда работать через PR** (см. CLAUDE.md), перед PR —
  `pytest` + `ruff` (backend) и `npm run build` (admin-panel).

## Как читать данные Bitrix24 (read-only)

Готовый инспектор смарт-процесса (через наш сервер, не напрямую в Б24):

```
GET https://hub.bsk-grupp.ru/api/admin/inspect/smart-process?entity_type_id=1066&item_id=481
Authorization: Bearer <ADMIN_API_TOKEN>
```

Возвращает: поля элемента (код|название|значение), определения полей,
товарные позиции, материнскую сделку (`parentId2`), ошибки вызовов Б24 — в JSON.
То же самое на сервере: `bash scripts/inspect-spa.sh 1066 481` (выполняется
внутри контейнера `app`; с хоста `localhost:8000` закрыт).

Ограничение Bitrix24: **роботы/триггеры/правила автоматизации через REST API не
читаются** — их разбираем только по скриншотам владельца.

## Текущая задача: workflow «Снабжение»

Смарт-процесс Б24 «Снабжение», `entityTypeId = 1066`. Пример элемента: id 481
(https://basht.bitrix24.ru/crm/type/1066/details/481/).

Бизнес-сценарий (со слов владельца, дословно по смыслу):

На этапе **«Счёт получен / отправлен в оплату»** в МойСклад создаётся цепочка:

1. **Заказ поставщику** (`purchaseorder`):
   - галочки «проведено» и «ожидание»;
   - Склад = поле **«Склад МС»** материнской сделки;
   - План. дата приёмки = поле **«Плановая дата готовности у поставщика»**;
   - Организация, Контрагент, товары — из элемента СПА.
2. На основании заказа — **Счёт поставщика** (`invoicein`):
   - «проведено»;
   - План. дата оплаты = поле **«Дата оплаты поставщику»**;
   - Входящий номер и дата = поле **«№ и дата счёта поставщика»**.
3. На основании счёта — **Приёмка** (`supply`):
   - на дату «План. дата приёмки»;
   - галочка «Проведено» — **снята**.

Реализация начата в `backend/src/workflows/create_supplier_docs.py` (создаёт
заказ+счёт в dry-run); требуется расширение до цепочки из 3 документов и
маппинга реальных полей.

### Следующий шаг

Прочитать инспектором элемент 1066/481, выписать реальные коды UF-полей СПА и
материнской сделки, сопоставить их с маппингом выше, обновить workflow,
показать владельцу dry-run превью. Записей не делать.

### Открытые вопросы владельцу

1. «Ожидание» в заказе поставщику МС — какое это поле (стандартного флага
   «ожидание» в API `purchaseorder` нет; возможно, имеется в виду статус/state)?
2. Как в СПА определяется поставщик (контрагент): companyId элемента или
   отдельное поле?
3. Как сопоставлять товары Б24 → МС: по артикулу, коду или названию?
4. Поле «Склад МС» в материнской сделке хранит название склада или id?

## Контекст предыдущих работ

- Подтверждено рабочее чтение боевого Б24 в dry-run (превью сделки «ТЕСТ-777»,
  товар «Соль таблетированная», сумма 5000) — записи не выполнялись.
- Владелец оставил в Б24 два исходящих события: обновление сделки и изменение
  элемента смарт-процесса. Вебхук-приёмник: `/webhooks/bitrix24`.
- Владелец пользуется приложением синхронизации Б24↔МС с курсом-мануалом:
  https://www.htmls.ru/learning/course/index.php?COURSE_ID=5&TYPE=Y — изучить
  как референс настроек, когда сеть открыта.
- Стиль общения с владельцем: простой язык, короткие пошаговые инструкции,
  не валить всё в кучу.
