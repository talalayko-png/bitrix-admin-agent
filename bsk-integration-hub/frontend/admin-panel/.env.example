# Base URL of the backend API. Leave empty to use the Vite dev proxy (recommended for local dev).
VITE_API_BASE=
# Optional: pre-fill the admin token. Otherwise it is entered in the UI and stored in localStorage.
VITE_ADMIN_TOKEN=
